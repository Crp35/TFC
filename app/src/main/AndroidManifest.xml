<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_icon_leo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/book_open_book_read_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.LectorLibros">

        <!-- MAIN ACTIVITY -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:screenOrientation="fullUser"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- IMPORTACIÓN DE PDFs: recibe el PDF desde fuera, lo guarda y lanza el lector -->
        <activity
            android:name=".ui.players.PdfImportActivity"
            android:label="@string/importar_pdf"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="application/pdf" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
            </intent-filter>
        </activity>

        <!-- LECTOR DE PDFs: solo se lanza desde dentro de la app, nunca desde fuera -->
        <activity
            android:name=".ui.players.PdfReaderActivity"
            android:label="Leer en Leo"
            android:exported="false">
            <!-- Sin intent-filter: elimina el conflicto con PdfImportActivity -->
        </activity>

        <!-- IMPORTACIÓN Y REPRODUCCIÓN DE AUDIOS -->
        <activity
            android:name=".ui.players.AudioImportActivity"
            android:label="@string/importar_audio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="application/ogg" />
            </intent-filter>
        </activity>

    </application>
</manifest>