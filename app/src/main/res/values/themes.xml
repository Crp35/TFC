<resources>

    <style name="Base.Theme.LectorLibros"
        parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Colores principales -->
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Fondo tipo Kindle (no negro puro) -->

        <item name="colorOnBackground">@color/white</item>

        <!-- Superficies (IMPORTANTE en Material3) -->
        <item name="colorSurface">#1A1A1A</item>

        <item name="colorSurfaceVariant">#202020</item>
        <item name="colorOnSurfaceVariant">@color/white</item>

        <!-- Botones y UI -->
        <item name="colorAccent">@color/colorPrimary</item>

        <!-- Sistema -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>

            <item name="colorPrimary">@color/colorPrimary</item>
            <item name="colorOnSurface">@color/white</item>
            <item name="android:textColor">@color/white</item>


    </style>

    <style name="Theme.LectorLibros"
        parent="Base.Theme.LectorLibros" />

</resources>